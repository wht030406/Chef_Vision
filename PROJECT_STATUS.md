# Chef Vision — 项目状态文档

更新时间：2026-05-26

---

## 项目目标

**最终目标：** 在炒菜机器上实现全自动实时菜表面温度监测，根据实时温度反馈调整机器的火候、翻转等指令，实现闭环控制。

**当前阶段：** 预研 + 数据采集，边写代码边用真实炒菜视频测试，尚未部署到下位机。

---

## 系统架构

```
厨房现场（低配笔记本）          开发机（RTX 5070 Ti）
─────────────────────          ──────────────────────────────
FieldCapture.py                TrackFood.py（离线处理）
  ├─ RGB 视频 rgb_{ts}.mp4  →    SAM2 食材追踪 → mask
  ├─ IR 视频  ir_{ts}.mp4        ↓
  └─ 温度矩阵 temp_{ts}.npy  →  温度融合（homography）→ CSV/Excel/曲线图
```

---

## 当前技术方案

### 食材追踪
- **算法：** SAM2（`sam2.1_hiera_tiny`）视频分割
- **模式：** 离线批处理，每批 100 帧（4 秒视频）
- **速度：** ~12 秒/批（RTX 5070 Ti），处理 4 分钟视频约需 10 分钟
- **精度：** 在无遮挡情况下 mask 准确，遮挡/投料时会漂移
- **模型文件：** `models/sam2.1_hiera_tiny.pt`（149MB）、`models/sam2.1_hiera_large.pt`（856MB，备用）

### 温度融合
- RGB mask → 单应矩阵（homography）→ IR 坐标 → 统计温度均值
- **当前问题：** `data/homography.npy` 是旧标定数据，RGB 四角全部越界，需要重新标定

### 数据采集（下位机）
- 脚本：`field/FieldCapture.py`
- 输出：`rgb_{ts}.mp4` + `ir_{ts}.mp4` + `temp_{ts}.npy`（三路同时间戳）
- 依赖：`numpy`、`opencv-python`，不需要 GPU

---

## 已知问题 & 待处理

| 优先级 | 问题 | 原因 | 解决方案 |
|--------|------|------|----------|
| 高 | 单应矩阵过期 | 摄像头位置变了 | 重新运行 `tools/Calibrate.py` 标定 |
| 高 | mask 漂移到投料盒 | 投料盒外观与菜相似，SAM2 追踪跑偏 | 在投料帧前后补关键帧（`food_labels.json` 加 keyframe） |
| 中 | 无法实时 | SAM2 固定约 15fps，处理速度 < 视频帧率 | 见下方"实时化路线"讨论 |
| 低 | 全自动标点 | 目前需要手动标注第一帧 | 待后续研究自动检测方案 |

---

## 实时化路线讨论（结论）

**背景：** 最终目标是实时监测，但 SAM2 在高端 GPU 上也只有 ~15fps，无法实时处理 25fps 视频。

**讨论过的方案：**

1. **降分辨率（640p）**：无效。SAM2 内部会把输入 resize 到 1024p，外部缩放不影响速度。
2. **换 tiny 模型**：有效但不够。tiny 比 large 快 1.5 倍，仍超时 3.5 倍。
3. **增大批次（100帧）+ 双线程流水线**：理论上可以实现 4 秒延迟，但实测 100 帧需 12 秒，仍超时 3 倍。
4. **换轻量追踪器（CSRT/KCF）**：速度够，但精度差，遮挡时容易丢失。
5. **IR 温度阈值分割**：锅壁温度极高（>200°C），菜温度相对低，可以直接阈值分割。延迟 <50ms，CPU 可跑。但炒菜后期菜和锅颜色接近，效果待验证。

**当前结论：**
- 预研阶段继续用 SAM2 离线处理，验证 mask 精度和温度数据质量
- 下次录制时同步录制 IR 视频（已实现），观察炒菜全程菜和锅的温度差异
- 实时化方案待 IR 视频数据分析后再决定

---

## 文件结构说明

```
Chef_Vision/
  field/          ← 下位机采集脚本（低配电脑用）
    FieldCapture.py     主采集脚本（RGB+IR视频+温度矩阵）
    TempMonitor.py      实时温度监控（独立工具）
    DataLogger.py       数据记录工具
  core/           ← 核心处理算法（开发机用，需要 GPU）
    TrackFood.py        SAM2 食材追踪 + 温度融合主程序
    LabelFirstFrame.py  第一帧标注工具
    food_labels.json    标注数据（关键帧坐标）
  sdk/            ← 热像仪 SDK 文件（DLL + Python 封装）
  models/         ← SAM2 模型权重（不提交 git）
  data/           ← 标定数据（homography.npy 等）
  test_data/      ← 测试用视频和温度数据
  output/         ← 处理结果（按时间戳子目录）
  tools/          ← 辅助分析工具
  backup/         ← 旧版本备份
```

---

## 近期 Git 记录

| commit | 说明 |
|--------|------|
| 4d612f1 | fix(field): FieldCapture 路径改为同目录（适配下位机） |
| ab6429c | feat(field): 新增 IR 视频录制功能 |
| bb12e76 | tools: 新增分析和测速脚本 |
| 544ff39 | chore: 迁移模型到 models/，换用 tiny 模型 |
| 2acb438 | 项目整理（目录结构重组） |
