# Chef_Vision 项目状态文档

> 最后更新：2026-04-30（第八次更新）
> 用途：每次新对话开始时读取此文件，恢复上下文，避免重复讨论

---

## 一、项目背景与最终目标

### 系统概述
炒菜机器人温度感知系统（Chef_Vision）。
硬件：**双光模组**（RGB摄像头 + 红外热像仪），目前手持采集数据，后期固定在炒菜机上。

### 最终目标
```
双光模组（RGB + IR）
    → 实时识别锅内食材区域
    → 获取食材表面真实温度
    → 对比预期温度曲线，判断火候是否足够
    → （后续）反馈给下位机，修正火候/翻炒时间等参数
```

**典型应用场景举例：**
同一套炒菜程序，常温环境的菜 vs 冰箱里拿出来的菜，后者火候不够。
系统需要检测到这个差异，触发下位机修正。

### 当前阶段
**预研阶段（离线验证）**：
数据采集与数据分析分开，先验证"能否准确获得食材表面温度"，再考虑实时化和边缘部署。

---

## 二、已完成的工作

| 步骤 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 1 | `LabelFirstFrame.py` | ✅ | 升级为多关键帧追加模式，支持 `--append --frame N --label` |
| 2 | `TrackFood.py` | ✅ | 分批处理解决OOM；支持多关键帧批内注入（inject_keyframes） |
| 3 | `food_labels.json` | ✅ | 迁移为多关键帧列表格式（新格式，旧格式向后兼容） |
| 4 | 首次运行 TrackFood.py | ✅ | 运行成功，发现跨批 mask 大幅跳变（已修复） |
| 5 | 修复跨批跳变 | ✅ | carry_mask 从"采样点"改为 add_new_mask() 直接传 mask 边界 |
| 6 | 重新运行 TrackFood.py（第一次） | ✅ | 跨批连续性完全修复，但发现标注策略问题（只标了肉，没标青椒） |
| 7 | inspect_frames.py 运行 | ✅ | 提取关键帧截图，诊断 mask 异常 |
| 8 | track_result.mp4 人工复核 | ✅ | 发现 SAM2 第一次追踪失败，原因已重新分析（见第四节） |
| 9 | analyze_ir_temp.py 运行 | ✅ | 生成 IR 温度统计 + 关键帧热图，获得真实温度数据（见第五节） |
| 10 | browse_video.py | ✅ | 新建：视频浏览工具，用于目视确认青椒入锅帧号 |
| 11 | **方案G 第二次追踪** | ✅ | **本次对话完成**（见第六节） |

---

## 三、关键文件路径

| 文件 | 说明 |
|------|------|
| `rgb_20260428_121157.mp4` | 主采集视频（约3335帧，FPS=25） |
| `temp_20260428_121546.npy` | 对应红外温度数据（shape: [5725, 192, 256]） |
| `homography.npy` | RGB→IR 坐标映射单应矩阵 |
| `food_labels.json` | 标注文件（**新格式**：2个关键帧，第780帧肉 + 第1830帧青椒） |
| `track_result.mp4` | SAM2 **第二次**追踪可视化视频（方案G，绿色mask，主体稳定） |
| `food_temp_log.csv` | **第二次**追踪温度日志（主体可信，末尾青椒变色段 mask=0） |
| `food_temp_curve.png` | **第二次**追踪温度曲线（主体可信） |
| `browse_video.py` | 视频浏览工具（找关键帧帧号用） |
| `LabelFirstFrame.py` | 标注工具（已升级为多关键帧追加模式） |
| `TrackFood.py` | SAM2 追踪主程序（已升级：多关键帧注入 + 无标注点数量限制） |
| `analyze_result.py` | 诊断脚本（统计mask分布和批次交接质量） |
| `inspect_frames.py` | 关键帧截图提取脚本 |
| `analyze_ir_temp.py` | IR 温度分布分析脚本 |
| `ir_analysis/` | IR 分析输出目录（热图、直方图、统计文本） |
| `D:/sam2_checkpoints/sam2.1_hiera_large.pt` | SAM2 权重 |

---

## 四、SAM2 追踪历史与失败分析

### 第一次追踪（失败，已弃用）
```
失败原因：标注策略问题
- 只标了肉（前景）+ 搅拌爪（背景）
- 青椒入锅时 SAM2 把青椒当"入侵物"→ mask 爆炸
- mask_ratio 从 0% 突然跳到 17%→57%
```

### 第二次追踪（方案G，本次对话完成）
**标注配置：**
```
关键帧1：第780帧（肉）
  fg_points = 12个（覆盖肉的不同部位）
  bg_points = 10个（锅底/锅外，不含搅拌爪）

关键帧2：第1830帧（青椒入锅，t=73.2s）
  fg_points = 23个（覆盖整个青椒区域）
  bg_points = 0个
```

**代码变更：**
- `TrackFood.py`：`MAX_FG_POINTS = None`，`MAX_BG_POINTS = None`（不再限制标注点数量）

**第二次追踪结果对比：**

| 阶段 | 帧号(rel) | 时间 | 第一次 mask% | 第二次 mask% | 结论 |
|------|-----------|------|-------------|-------------|------|
| 肉翻炒正常段 | 0~1000 | 31~71s | 0.1%~0.7% | 0.6%~8.7% | ✅ 均稳定，无爆炸 |
| 青椒入锅前后 | 1190~1200 | 78.8~79.2s | 0%→17%（爆炸） | 2.9%~3.6% | ✅ 完全修复！ |
| 青椒翻炒后期 | 1400~1800 | 87~103s | 40%~50%（爆炸） | 3.9%~4.8% | ✅ 正常稳定 |

**⚠️ 新发现的问题：青椒变色导致 mask 消失**
```
视频末尾 7~8 秒：青椒由绿色 → 炒熟变灰黑色
SAM2 memory 记住的是"绿色青椒"外观
颜色变化太大 → SAM2 认不出 → mask 归零
此外，zero_rel0541_t52.8s_mask0.0pct.jpg 也出现过短暂消失（肉翻炒中期）
```

**📋 第七次更新：人工复核视频后的完整缺陷描述（用户目视评审）**

| 阶段 | 现象 | 严重程度 | 备注 |
|------|------|----------|------|
| 炒肉前段（t=31s~约68s） | mask 准确覆盖肉区域，搅拌爪成功分隔（不被 mask） | ✅ 良好 | 符合预期 |
| 加酱油前 4~5s（约 t=68~73s） | mask 跑到搅拌爪上，误追踪搅拌爪 | ⚠️ 中等 | 单段约4~5s |
| 加酱油后（肉变黑）| 肉颜色由白变黑，mask 自动重新追上肉 | ✅ 意外惊喜 | SAM2 自适应恢复 |
| 青椒入锅后整体段 | 整体还行，基本 mask 到菜上，同时分隔搅拌爪 | ✅ 可接受 | 无大面积误追踪 |
| 视频末尾（青椒+肉混炒后段） | mask 消失，可能受轻微烟雾干扰，不只是变色问题 | ⚠️ 重要 | 烟雾+变色双重干扰 |

**优化目标：**
```
预研阶段：追求极致准确性，误差越小越好，不因"整体还行"而停止优化
当前结论：整体结果不错，有几处值得继续优化的缺陷
```

---

## 五、IR 温度分布验证结论（已确认，基于真实数据）

### analyze_ir_temp.py 运行结果（ir_analysis/ 目录）

| 帧号 | 均值 | 最大值 | std | 直方图特征 | 对应场景 |
|------|------|--------|-----|-----------| --------|
| 0 | 12.0°C | 83.4°C | 10.3°C | 单峰，室温 | 空锅开机前 |
| 100 | 15.6°C | 116.6°C | 17.7°C | 单峰+少量高温 | 预热初期 |
| 600 | 38.5°C | 254.8°C | 53.3°C | 左峰(低温)+右尾(高温) | 烹饪中期 |
| 1200 | 13.4°C | 94.0°C | 4.8°C | 单峰，温度骤降 | 摄像头移开/对准其他区域 |
| 2000 | 92.7°C | 294.4°C | 78.0°C | 明显双峰（20~50°C + 150~250°C） | 烹饪后期 |

**全局统计：** 总帧数=5725，IR分辨率=256×192，最高温=304.35°C，全局均值=58.96°C

### 关键发现1：温度骤降 = 摄像头移开
温度曲线中多次出现骤降至~15°C，不是食材冷却，而是手持拍摄过程中摄像头短暂移开或对准其他区域。

### 关键发现2：双峰的低温峰不是食材
frame=2000 的低温峰（20~50°C）= 画面中锅以外的厨房背景（手持拍摄，视野比锅大很多）。
高温区（150~250°C）= 锅壁。锅壁和食材的温差在烹饪后期依然很小。

### 关键发现3：IR 阈值分割食材 vs 锅壁在后期失效
主观观察 web 端 IR 渲染视频：
- 刚下锅：食材（深蓝/低温）vs 锅壁（红色/高温），温差大，可区分
- 烹饪后期：食材和锅壁颜色趋同（铁锅导热性强，物理规律），无法用阈值区分

---

## 六、技术方向分析与当前决策

### 已排除方案

| 方案 | 排除原因 |
|------|---------|
| IR 阈值分割食材 vs 锅壁 | 烹饪后期温差消失（物理规律），后期失效 |
| 纯 IR 锅外区域低温峰分析 | 手持拍摄画面含锅外区域，低温峰不代表食材 |

### ✅ 方案G 第二次追踪结果评估

**已成功修复的问题：**
- mask 爆炸（青椒入锅时）→ 完全修复
- 跨批跳变 → 早已修复

**残留问题（待决策）：**
```
问题1：加酱油前 4~5s 搅拌爪误追踪（约 t=68~73s）
  → 原因：酱油未加时肉与搅拌爪颜色对比度下降，SAM2 混淆目标
  → 方案：browse_video.py 找准确帧号 → 追加关键帧标注（加酱油前该段）→ 重跑追踪
  → 注意：加酱油后 SAM2 已自动恢复，说明颜色是主因

问题2：视频末尾青椒+肉混炒后段 mask 消失（末尾约 7~8s）
  → 原因：青椒变色（绿→灰黑）+ 轻微烟雾，双重干扰
  → 方案C1：追加"青椒变色"关键帧，在变色前那一帧追加标注，重跑追踪
  → 方案C2：接受现有结果，末尾几秒数据缺失，主体 73s~约115s 已有效

问题3：肉翻炒中期偶有 mask=0 短暂消失（rel帧约541，t=52.8s）
  → 可能是摄像头抖动/手持偏移，或肉被搅拌爪短暂完全遮挡
  → 影响有限，可接受
```

### 🔄 备选方案E：SAM2 追踪锅口 ROI（方案G彻底失败时启用）

```
SAM2 追踪目标改为：锅口圆形边缘（黑色，形状稳定，不被遮挡）
→ 将锅口 mask 投影到 IR
→ 统计锅内整体温度（精度较低，但稳定）
```

---

## 七、下一步行动计划

### 🔴 新对话开始后立即执行的任务（按优先级排序）

**目标：解决以下两个主要缺陷**
1. 加酱油前 4~5s 搅拌爪误追踪（约 t=68~73s）
2. 视频末尾 mask 消失（末尾约 7~8s，烟雾+变色双重干扰）

---

**缺陷1 修复方案：追加"加酱油前"关键帧**
```
步骤1：用 browse_video.py 找加酱油前搅拌爪误追踪开始的帧号
  python browse_video.py
  → 找到 mask 跑到搅拌爪上的起始帧（约 t=68~73s 附近，约第 1700~1825 帧）
  → Space 记录帧号，Q 退出

步骤2：追加该段关键帧标注（标肉为前景，搅拌爪为背景）
  python LabelFirstFrame.py --append --frame XXXX --label "加酱油前"
  → 左键点击肉区域（前景）
  → 右键点击搅拌爪（背景，明确排除）
  → S 保存

步骤3：重新运行追踪
  python TrackFood.py

步骤4：验证结果
  python inspect_frames.py
```

**缺陷2 修复方案C1（推荐）：追加"青椒变色"关键帧**
```
步骤1：用 browse_video.py 找青椒开始变灰色的帧号
  python browse_video.py
  → 视频末尾约 7~8 秒前（约第 3150~3200 帧附近，视频总长~3335帧）
  → Space 记录帧号，Q 退出

步骤2：追加变色青椒关键帧标注
  python LabelFirstFrame.py --append --frame XXXX --label "青椒变色"
  → 左键点变灰色的青椒区域（前景点）
  → 不需要背景点
  → S 保存

步骤3：重新运行追踪（可与缺陷1修复合并一次重跑）
  python TrackFood.py

步骤4：验证结果
  python inspect_frames.py
  → 确认末尾 mask 是否稳定
```

**缺陷2 修复方案C2（备选）：接受现有结果**
```
直接查看 food_temp_curve.png 分析温度曲线
食材温度数据有效范围：t=31.2s ~ 约 125s（末尾缺失）
```

### 后续（第二优先级）
- [ ] 如果方案G/C1成功：输出完整食材温度曲线（肉+青椒全程）
- [ ] 如果方案G仍然失败：改用方案E（SAM2追踪锅口 + IR统计）
- [ ] 对比常温 vs 冰箱食材的温度曲线差异

### 再后（第三优先级）
- [ ] 设计火候判断逻辑（阈值/斜率/积分等）
- [ ] 实时化方案设计（边缘计算部署）
- [ ] 下位机接口设计

---

## 八、拍摄数据说明（重要）

| 项目 | 说明 |
|------|------|
| 拍摄方式 | **手持拍摄**，视野包含锅及周围厨房设备，锅口位置在视频中会变化 |
| 拍摄流程 | 空锅（~10s）→ 热锅 → 加肉翻炒 → 加调料（肉变黑）→ 加青椒（t=73.2s，第1830帧）→ 一起翻炒 → 青椒变灰（末尾7~8s）→ 结束 |
| temp.npy 第0帧 | 锅开启前的空锅状态（均值~12°C，室温） |
| 温度骤降原因 | 手持过程中摄像头短暂对准锅外区域，温度曲线出现多次骤降至~15°C |
| 双峰低温峰来源 | 不是食材，而是画面中锅以外的厨房背景区域（室温） |
| 总帧数对应 | temp.npy 共 5725 帧，RGB 视频约 3335 帧（帧率不同，需对齐） |

---

## 九、硬件与环境信息

| 项目 | 详情 |
|------|------|
| GPU | RTX 5070 Ti 16GB |
| OS | Windows 11 |
| Python 环境 | SAM2, OpenCV, NumPy, PyTorch, Matplotlib |
| 双光模组 | RGB + 红外热像仪（同步采集） |
| 当前采集方式 | 手持（后期固定在炒菜机上） |
| 炒菜机 | 搅拌爪细长旋转翻炒，白色，加调料后与变黑食材颜色差异变大 |

---

## 十、关键设计决策记录

| 决策 | 原因 |
|------|------|
| 分批处理（CHUNK_SIZE=200）| 解决 SAM2 视频追踪 OOM 问题 |
| 跨批用 add_new_mask() 而非采样点 | SAM2 源码中 mask 和 point 在同帧互斥，用 mask 传递边界更精确 |
| 第一次 SAM2 追踪失败原因重新评估 | 不是算法结构性失败，而是标注策略问题（只标了肉、没标青椒） |
| 废弃"IR 阈值分割食材 vs 锅壁" | 烹饪后期食材温度趋近锅壁温度（物理规律），后期无法区分 |
| 搅拌爪容许混入 mask | 排除困难，混入代价（温度误差小）< 排除失败代价（mask 不稳定） |
| 搅拌爪不标前景也不标背景 | 中性放任：爪子旋转导致背景点失效；不标则 SAM2 大概率自行忽略 |
| 改进策略：多关键帧标注 | 在青椒入锅关键帧追加前景点，避免 SAM2 把青椒当入侵物 |
| food_labels.json 多关键帧格式 | 支持多个食材入锅节点的标注，向后兼容旧格式 |
| LabelFirstFrame.py --append 模式 | 追加而非覆盖，保留已有关键帧标注 |
| TrackFood.py inject_keyframes | 批内按帧号注入额外前景点，SAM2 memory 同时包含多种食材特征 |
| MAX_FG_POINTS = None（无限制）| 原来限制8个点，大面积青椒区域被截断，改为不限制使用全部标注点 |
| 目标食材范围 | 锅内所有食材（肉+青椒等蔬菜），搅拌爪可容许混入 |
| 当前阶段 | 离线预研，不考虑实时化，先验证温度获取可行性 |

---

## 十一、food_labels.json 当前标注内容（第二次追踪使用）

```json
关键帧1：frame=780（t=31.2s）  标签="初始标注"
  fg_points: 12个（肉区域，分散覆盖）
  bg_points: 10个（锅底/锅外，不含搅拌爪）

关键帧2：frame=1830（t=73.2s）  标签="青椒入锅"
  fg_points: 23个（覆盖整个青椒区域）
  bg_points: 0个
```

---

## 十二、mask 准确性与稳定性优化方向（后续工作重心）

> 目标：最终产品能够自适应、自动化地追踪锅内食材，无需人工干预。
> 以下方向均需满足"可自动化、可产品化"的前提，预研阶段先验证可行性。

### 方向A：动态关键帧自动注入
- 核心思路：监控 mask 面积变化，检测到追踪丢失信号时，自动从当前帧生成新的前景点并重新注入 SAM2 memory
- 适用场景：食材颜色变化（加酱油变黑、青椒炒熟变灰）、烟雾遮挡等导致的 mask 丢失
- 产品化可行性：✅ 完全可自动化，无需人工

### 方向B：新食材入锅自动检测与标注
- 核心思路：检测锅内区域出现新食材的事件（帧差法或 IR 温度骤降），自动对新区域生成前景点并追加到追踪目标
- 适用场景：加青椒、加其他配料等新食材入锅节点
- 产品化可行性：✅ 完全可自动化，IR 骤降是天然的入锅信号

### 方向C：后处理时序修复
- 核心思路：对 SAM2 输出的 mask 序列做时间轴后处理，检测并修复孤立的异常帧（短暂消失或短暂爆炸）
- 适用场景：手持抖动、搅拌爪短暂遮挡等导致的单帧或少数帧异常
- 产品化可行性：✅ 纯后处理，不影响追踪本身

### 方向D：IR 辅助验证 mask
- 核心思路：将 RGB mask 投影到 IR 坐标系后，检查 mask 区域的温度分布是否合理；若出现异常（如温度极高说明 mask 压到锅壁，或温度极低说明 mask 跑到锅外），自动标记该帧为可疑帧并触发重注入
- 适用场景：搅拌爪误追踪、mask 跑到锅壁等错误目标
- 产品化可行性：✅ 利用 IR 数据做交叉验证，是本项目独有优势

### 方向E：输入预处理优化
- 核心思路：在 RGB 帧输入 SAM2 之前做图像增强，减少烟雾、光照变化等对颜色识别的干扰
- 适用场景：烹饪末期烟雾干扰、强光反射等
- 产品化可行性：✅ 可集成到实时处理流水线

### 当前预研阶段执行策略
```
手动追加关键帧  →  验证"动态注入"思路的有效性（方向A/B 的人工版本）
后处理脚本      →  验证时序修复的效果（方向C）
IR 温度异常检测 →  验证交叉验证的可行性（方向D）
以上验证通过后  →  逐步将人工操作替换为自动化逻辑
```

---

---

## 十三、方案 AutoTrack：全自动自适应食材追踪系统

> 方案名称：**AutoTrack**
> 讨论时间：2026-04-30（第八次更新）
> 状态：**设计阶段，尚未实现**
> 目标：完全无需人工标注（food_labels.json），相机对准锅即可自动追踪所有食材，实时输出温度

---

### 背景与动机

当前 `TrackFood.py` 的架构是「分批批处理」（每批200帧），追踪完成后才能看到 mask，无法实时干预。
最终产品需要实时逐帧输出食材温度（每帧延迟 < 几百毫秒），批处理架构不可行。

AutoTrack 方案将架构改造为「逐帧 step」模式，同时集成自动化的食材检测与追踪恢复能力。

---

### 功能分解（对应原方向A + 方向B）

| 子功能 | 对应方向 | 触发时机 | 前景点 | 背景点 |
|--------|----------|----------|--------|--------|
| 新食材入锅自动检测与首次标注 | 方向B | 检测到帧差/IR温度骤降 | ✅ 自动生成（覆盖新区域） | ✅ 首次需要（锅底/锅外） |
| 追踪丢失后自动恢复 | 方向A | mask面积连续骤降至阈值以下 | ✅ 从历史mask采样 | ❌ 不加（SAM2 memory已有） |

**背景点策略说明：**
- 第一次食材入锅：必须加背景点，告诉 SAM2 锅底/锅外不是食材
- 后续新食材入锅（如青椒）：不加背景点，SAM2 memory 已有背景记忆
- 追踪丢失重注入：不加背景点，同上

---

### 三步实施计划

#### Step 1：架构改造（基础，方向A和方向B共同依赖）
```
目标：把 TrackFood.py 改为逐帧 step 架构

当前架构（批处理）：
  批N：抽200帧 → SAM2 propagate_in_video() → 得到200帧masks → 写视频
  问题：追踪完才能看mask，无法实时干预

目标架构（逐帧）：
  for frame in video:
      mask = predictor.step(frame)   ← 单帧推理，立即得到mask
      check_mask(mask)               ← 立即检查面积
      output_temp(mask, ir_frame)    ← 立即输出温度

技术要点：
  - 使用 SAM2 的逐帧推理 API（研究 sam2_video_predictor.py 内部接口）
  - 维护 SAM2 的 memory bank（替代当前的 carry_mask 机制）
  - 保留分批抽帧（避免OOM），但每批内改为逐帧推进

待确认细节：
  - SAM2 逐帧 API 的具体接口名称和参数（需研究源码）
  - 内存管理策略（memory bank 大小限制）
```

#### Step 2：方向A 自动追踪恢复
```
目标：检测到 mask 消失时，自动重注入前景点恢复追踪

检测逻辑：
  - 维护滑动窗口（最近 N 帧的 mask_ratio）
  - 触发条件：连续 K 帧 mask_ratio < 阈值（如 < 1%，连续5帧）

自动前景点生成：
  - 使用最后一次有效 mask 的区域
  - 形态学腐蚀（避免采到边缘）
  - 均匀采样 N 个点（N 根据面积自适应，公式：N = max(3, min(50, area/500))）

重注入：
  - 只注入前景点，不加背景点
  - 在当前帧 add_new_points() 触发 SAM2 重新建立追踪

验证目标（对应已知缺陷）：
  - 末尾7~8s mask消失（青椒变色+烟雾）→ 应能自动恢复
  - 肉翻炒中期偶有消失（rel帧~541）→ 应能自动恢复

待讨论细节：
  - 触发阈值（面积 < ?%，连续?帧）需根据实验调整
  - 重注入后是否需要强制传播几帧以稳定 memory
```

#### Step 3：方向B 新食材入锅自动检测
```
目标：无需 food_labels.json，自动识别食材入锅并建立追踪

检测信号（优先级排序）：
  1. IR温度骤降（首选）：新食材（冷）落入热锅，IR某区域温度骤降
     → 优点：对搅拌爪运动不敏感（金属爪不会导致骤降）
     → 骤降判定：某区域 ΔT < -20°C/帧 且 持续2帧以上
  2. RGB帧差法（备选）：两帧之差超过阈值的区域
     → 需要过滤搅拌爪运动（细长条形 vs 食材团状，形态学区分）

自动前景点生成（首次）：
  - 取骤降/帧差区域的连通域
  - 形态学腐蚀 + 面积自适应采样 fg_points
  - 第一次入锅还需 bg_points：在锅外/锅底区域采样背景点

与方向A的配合：
  - 方向B 触发后，方向A 开始监控新食材的 mask 面积
  - 两者共用同一个逐帧 step 循环

最终效果（Step3完成后）：
  - 开机 → 相机对准锅 → 全程自动，无需任何人工标注
  - 自动识别肉入锅、青椒入锅等所有食材节点
  - 实时输出每帧食材温度

待讨论细节：
  - IR骤降阈值的标定（需要更多拍摄数据验证）
  - 搅拌爪运动的形态学过滤方案
  - 多种食材同时变化时的区域分割策略
```

---

### 各步骤依赖关系

```
Step1（逐帧架构）
    ├─→ Step2（方向A：追踪恢复）
    └─→ Step3（方向B：新食材检测）

Step2 和 Step3 可并行开发，最终合并到同一逐帧循环中
```

### 当前状态与下一步

- [ ] Step1：研究 SAM2 逐帧 API，重构 TrackFood.py
- [ ] Step2：实现 mask 面积监控 + 自动重注入
- [ ] Step3：实现新食材入锅检测 + 自动首次标注

---

*本文档由 AI 助手自动生成，每次对话结束后应更新此文档。*
