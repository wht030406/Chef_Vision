# 后续优化方向

结合近期对 RGB 正向、RGB 反向和 IR 方案的讨论与测试，后续优化方向可简洁归纳如下。

## 1. 锅区与旋转轴排除圆的定位追踪精度

目前锅区会尝试动态更新，但稳定性还不是特别理想；旋转轴排除圆主要依赖手动标注和简单跟随。遇到拍摄位置变化、锅姿态变化时，排除圆可能偏离真实旋转轴，进而影响 RGB 正向采点和 axis_stuck 判定。

## 2. RGB 正向重采点策略继续精修

当前 RGB 正向已经接入 IR 分割、core 区、分散采样、轴心排除、灰度过滤等机制，但仍可能出现采点靠近旋转轴、贴边或采到不理想区域的问题。后续可继续优化 FG 点更集中在食材主体，BG 点更稳定避开干扰区。

## 3. RGB 正向异常判定更贴合实际视频

当前已有面积过大、面积过小、骤降、轴心驻留、上半锅区驻留等判断，但不同视频中的异常形态不完全一致。后续可继续根据完整视频触发情况微调阈值，减少漏判和误判。

## 4. 批内 IR-fix / 批内重置 SAM2 机制可作为后续高精度方向

当前主线采用每个 chunk 开头参考重检与 IR_relabel，RGB 正向批内 IR-fix 和 RGB 反向批内 IR 兜底纠正机制暂时关闭但保留。现有批内纠正思路主要是把当前帧异常 mask 临时替换为 IR 分割结果映射到 RGB 后的 mask，并不会在批内真正重启或重置 SAM2，因此只能修正单帧输出，不能从根本上恢复后续追踪状态。此外，该方式本质上借用了 IR 分割得到的区域，再映射到 RGB 后用于温度计算，最终仍需回到 IR 温度数据上统计，工程价值需要谨慎评估。后续如果对实时精度要求更高，更值得考虑的是在批内发现异常后重新生成提示点并触发 SAM2 重启或局部重置，而不是只替换当前帧 mask。

## 5. IR-IoU 语义一致性检查可温和启用

当前保留了 chunk 开头 RGB carry_mask 与 IR 食材分割区域的重叠一致性检查思路，但默认关闭。后续可考虑将其改成更温和的机制，例如连续多帧或多批次低重叠才触发，或仅作为异常预警，避免因单帧 IR 分割波动导致频繁 IR_relabel。

## 6. 锅直立/白烟/空锅冻结机制保留为特殊场景方向

当前保留了锅直立、白烟遮挡或空锅状态下冻结 carry_mask、跳过补强或跳过本批 SAM2 的机制，但默认关闭。后续如果处理未剪辑的原始视频或设备运行中的极端姿态，可重新评估该机制，用于避免在食材不可见或锅壁占主导时继续输出错误 mask。

## 7. IR 分割策略保留 two-cluster 备选方案

当前 IR 分割主线已统一到 percentile 40% 方案。从目前测试效果看，该策略比 k-means/two-cluster 低温簇方案更加稳定，尤其是在 RGB 正向/反向 IR_relabel 采点、IR 展示 mask 和 IR 温度统计中表现更一致。two-cluster/K-means 仍可作为备用分割思路保留，但默认关闭，后续仅在 percentile 方案遇到特定场景不稳定时再重新评估。

## 8. RGB 反向方案继续作为辅助方案评估

RGB 反向目前主要作为辅助温度和辅助对照方案，整体逻辑已经简化。后续可继续观察它是否能稳定提升结果；如果贡献不明显，可以进一步弱化为调试参考。

## 9. SAM2 + 光流混合传播可作为实时化加速备选

当前主线仍采用纯 SAM2 分批追踪，OPTICAL_FLOW_INTERVAL = 0，光流传播保持关闭。光流传播的作用是利用相邻 RGB 帧之间的像素运动，把上一帧 mask 近似推到当前帧，从而减少 SAM2 的逐帧计算压力，理论上可用于提升近实时处理速度。但在当前炒菜场景中，食材翻滚变形、锅壁反光、遮挡、旋转轴和铲具快速运动都会影响光流稳定性，容易把 mask 带偏。因此该机制暂时只作为后续实时化加速方向保留，不作为当前稳定输出主线。
