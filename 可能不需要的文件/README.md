# 可能不需要的文件说明

本目录存放已经退出当前主线、但尚未最终决定删除的少量文件。它与
`旧代码库/` 的区别是：这里的文件已被初步判断为低价值候选，保留只是为了
避免过早删除。

| 文件 | 说明 | 当前替代 |
|---|---|---|
| `DataLogger.py` | 早期采集/记录脚本 | `field/FieldCapture.py` |
| `field_README_旧版.txt` | 旧现场说明 | `field/README.txt` 和 `field/TempMonitor使用说明.md` |
| `旧文件整理清单.md` | 2026-07-24 整理前的候选清单 | 当前目录结构及根目录总说明 |

静态检查未发现当前 `core/`、`field/` 或 `tools/` 导入这些文件。

## 使用原则

- 默认不运行、不修改。
- 删除前再次确认没有人工需要的历史信息。
- 若保留，应继续放在本目录，不要混回当前主线。
- `旧文件整理清单.md` 中部分路径已经因归档整理而变化，阅读时以当前目录
  结构为准。

