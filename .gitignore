# 大文件（视频、温度矩阵）
*.mp4
*.npy
*.avi

# 设备标定矩阵需要随公司交接版仓库保留
!data/homography.npy

# 输出目录只保留说明/占位，真实结果由本地运行生成
output/**
!output/
!output/README.md
!output/.gitkeep

# 临时帧目录
core/tmp_sam2_frames/

# SAM2 模型权重用于公司交接版仓库，需通过 Git LFS 跟踪。

# 测试数据目录只保留说明/占位，真实采集数据由本地准备
test_data/**
!test_data/
!test_data/README.md
!test_data/.gitkeep

# 本地运行标注状态（重新手动标注时会生成/更新，不作为发布版固定配置）
core/food_labels.json
data/wok_region.json

# Python 缓存
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# 日志
*.log
IRCNETSDK.log

# 系统文件
.DS_Store
Thumbs.db

# 标定图片（中间产物）
*.png
calibration_verify.png
filter_result.png
segment_result.png
temp_verify.png

# tools/ 调试可视化输出图（一次性产物，不入库）
tools/*.jpg

# Historical archive document retained locally; exceeds GitHub's normal file limit.
旧代码库/way.docx
backup/way.docx

